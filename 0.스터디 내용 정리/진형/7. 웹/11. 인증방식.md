# 🔐 웹 인증(Authentication) 방식 정리

---

## 📘 1. 인증(Authentication)이란?

**인증**은 “사용자가 누구인지 확인하는 절차”를 의미한다.  
즉, 시스템이 **로그인 요청을 보낸 사용자가 정말 그 사람인지** 검증하는 과정이다.

> 💡 간단히 말해, “너 진짜 그 사람이 맞아?”를 확인하는 절차.

---

## 🧩 2. 인증 vs 인가 vs 식별

| 구분 | 의미 | 예시 |
|------|------|------|
| **식별 (Identification)** | 사용자가 누구인지 밝힘 | “저는 user123입니다.” |
| **인증 (Authentication)** | 식별한 사용자가 진짜임을 확인 | “비밀번호를 입력하세요.” |
| **인가 (Authorization)** | 접근 가능한 자원과 권한 결정 | “관리자만 접근 가능” |

---

## 🧠 3. 주요 인증 방식 개요

| 방식 | 설명 | 예시 |
|------|------|------|
| **Basic / Digest 인증** | HTTP 프로토콜 기반 단순 인증 | ID/PW를 Base64로 인코딩 |
| **세션 기반 인증(Session)** | 로그인 시 서버에 세션 저장 | JSP, PHP, Spring MVC |
| **토큰 기반 인증(Token)** | 서버가 토큰 발급, 클라이언트가 보관 | JWT, OAuth 2.0 |
| **OAuth 2.0 인증** | 타 서비스의 자원 접근 허용 | 구글 로그인, 카카오 로그인 |
| **SSO (Single Sign-On)** | 한 번 로그인으로 여러 서비스 이용 | 구글 계정으로 YouTube, Gmail 사용 |
| **MFA (Multi-Factor Authentication)** | 복수 인증 수단 사용 | 비밀번호 + OTP, 생체인증 |

---

## 💡 4. Basic / Digest Authentication

### 📘 Basic 인증
- HTTP 헤더에 **ID:PW** 를 Base64 인코딩하여 전달.
- 예시:
  ```http
  Authorization: Basic dXNlcjpwYXNzd29yZA==
  ```
- 단점:
    - Base64는 암호화가 아니므로 탈취 시 위험.
    - HTTPS가 필수.

### 📘 Digest 인증
- 서버가 난수(`nonce`)를 발급하고, 클라이언트는 이를 포함해 해시 전송.
- 비밀번호 평문 전송은 막지만, 현대에는 거의 사용되지 않음.

---

## 🧾 5. 세션(Session) 기반 인증

### 🔹 동작 방식
1. 사용자가 로그인하면 서버가 세션 ID를 생성.
2. 이 세션 ID를 쿠키에 저장하여 클라이언트로 전송.
3. 이후 요청마다 세션 ID를 포함 → 서버가 세션 테이블에서 사용자 정보 조회.

### 🔹 특징
| 장점 | 단점 |
|------|------|
| 서버 제어로 보안적 | 서버 메모리에 세션 누적 → 확장성 낮음 |
| 세션 무효화 가능 | 분산 서버 환경에서 세션 공유 필요 |

---

## 🧾 6. 토큰(Token) 기반 인증

### 🔹 개념
서버는 로그인 시 **서명된 토큰(JWT 등)** 을 발급하고,  
클라이언트는 이 토큰을 보관해 이후 요청 시 `Authorization` 헤더에 포함한다.

```http
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### 🔹 장점
- 서버가 상태(State)를 유지하지 않아도 됨 (Stateless)
- 분산 서버, 마이크로서비스 구조에 적합

### 🔹 단점
- 토큰 유효 기간 동안 탈취 시 위험
- 토큰 강제 폐기가 어려움

---

## 🧭 7. OAuth 2.0 인증

**OAuth 2.0**은 비밀번호를 직접 제공하지 않고,  
사용자가 타 서비스(구글, 네이버 등)에 로그인하여 **Access Token**을 받아 사용하는 구조.

| 구성요소 | 설명 |
|----------|------|
| Resource Owner | 사용자 |
| Client | 접근 요청 앱 |
| Authorization Server | 인증 및 토큰 발급 서버 |
| Resource Server | API 제공 서버 |

> ✅ “비밀번호는 주지 않고, 서비스 접근 권한만 위임한다.”

예:  
카카오 계정으로 다른 앱 로그인 (→ 카카오 서버에서 토큰 발급)

---

## 🧱 8. SSO (Single Sign-On)

**SSO**는 한 번의 로그인으로 여러 시스템에 접근 가능하게 하는 기술이다.

### 🔹 구조
1. 공통 인증 서버(Authentication Server) 존재.
2. 로그인 성공 시 SSO 토큰/쿠키를 발급.
3. 같은 인증 도메인을 공유하는 서비스들은 자동 로그인 처리.

### 🔹 예시
- 구글 계정 1회 로그인 → Gmail, YouTube 자동 로그인
- 기업 내부 시스템 통합 로그인 (Active Directory, SAML 기반)

---

## 🔐 9. MFA (Multi-Factor Authentication)

**MFA**는 2개 이상의 인증 수단을 조합하는 고강도 보안 방식이다.

| 인증 요소 | 예시 |
|------------|------|
| **지식 기반 (Something you know)** | 비밀번호, PIN |
| **소유 기반 (Something you have)** | OTP, 스마트폰, 보안카드 |
| **존재 기반 (Something you are)** | 지문, 얼굴 인식, 홍채 |

예:  
비밀번호 입력 후 → Google OTP 인증 → 로그인 성공 ✅

---

## ☁️ 10. 현대 서비스의 인증 조합 예시

| 서비스 | 인증 방식 | 특징 |
|---------|------------|------|
| **네이버/카카오 로그인** | OAuth 2.0 + JWT | 비밀번호 공유 없이 외부 서비스 접근 |
| **은행 앱** | OAuth + MFA | 인증서, 생체인증 등 다단계 인증 |
| **사내 인트라넷** | SSO (SAML 기반) | 한 번 로그인으로 전사 시스템 접근 |

---

## 🧠 11. 핵심 요약

- **세션 기반 인증** → 서버 중심, 상태 유지 필요
- **토큰 기반 인증** → 클라이언트 중심, 확장성 높음
- **OAuth 2.0** → 외부 리소스 접근용 위임 인증
- **SSO** → 여러 서비스 통합 로그인
- **MFA** → 복합 인증으로 보안 강화

> 🧩 현대 서비스는 “OAuth 2.0 + JWT + MFA” 형태의 하이브리드 인증 방식을 주로 사용한다.

---

📘 **References**
- [RFC 6749 - OAuth 2.0 Authorization Framework](https://datatracker.ietf.org/doc/html/rfc6749)
- [RFC 7519 - JSON Web Token](https://datatracker.ietf.org/doc/html/rfc7519)
- [OWASP Authentication Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Authentication_Cheat_Sheet.html)
